/**
 * Static HTML generation module
 */

export async function generateSite(outputDir: string): Promise<void> {
  console.log(`Generating static site in ${outputDir}`);
  // TODO: Implement site generation
}
